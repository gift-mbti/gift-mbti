import styled from '@emotion/styled';
import SEO from '../components/SEO';

const Nav = styled.nav``;
const NavButton = styled.button``;

const results = () => {
  return (
    <div>
      <SEO type="모든 유형 한번에 보기" />
      <Nav>
        <NavButton>1</NavButton>
      </Nav>
    </div>
  );
};

export default results;
